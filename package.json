{
  "dependencies": {
    "gitbook-plugin-jsfiddle": "^1.0.0",
    "gitbook-cli": "^2.1.3"
  },
  "scripts": {
    "postinstall": "gitbook install && gitbook build && npm run path && npm run pdf",
    "path": "PATH=~/calibre:$PATH",
    "pdf": "gitbook pdf ./ ./student-gitbook.pdf"
  }
}
